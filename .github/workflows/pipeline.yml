name: My First Github Pipeline

on:
  push: 
    branches: 
       - main

jobs:
  hello:
    runs-on: ubuntu-latest 

    steps:
      - name: checkout code 
        uses: actions/checkout@v4

      - name: Say Hello
        run: cat app.txt